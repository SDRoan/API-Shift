# One click deploy on Render's free tier.
services:
  - type: web
    name: apishift
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: APISHIFT_MAX_CONCURRENT
        value: "2"
